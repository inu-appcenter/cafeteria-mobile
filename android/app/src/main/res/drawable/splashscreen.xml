<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
    보통 안드로이드 앱 같으면 테마 android:windowBackground에 이걸 지정해서
    스플래시로 쓰겠지만, 우리는 react-native-splash-screen을 쓰기 때문에
    launch_screen.xml에서 이걸 가져다 쓰는 용도밖에는 안 됩니다 ㅎㅎ
    -->
    <item android:drawable="@color/splashprimary" />
    <item
        android:drawable="@drawable/header_logo"
        android:gravity="center" />
</layer-list>
